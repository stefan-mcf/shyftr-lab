# Synthetic multi-cell fixture

This directory contains public-safe seed material for the multi-cell milestone multi-cell example. The automated demo test builds temporary cells from code so the repository does not need to track runtime ledger hashes or generated grid artifacts.

Suggested cells:

- `project-alpha`: approved memory about bounded API retry backoff.
- `project-beta`: similar approved memory about bounded API retry backoff.

Use `docs/multi-cell-example.md` for the CLI sequence and public-safe synthetic proof flow.
